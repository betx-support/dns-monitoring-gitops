# Referenced by vault-secretstore.yaml's serviceAccountRef and by
# 07-dns-monitoring-kubernetes-auth.sh's bound_service_account_names —
# both must match this name/namespace exactly, same as snipeit-eso.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dns-monitoring-eso
  namespace: dns-monitoring
